for N in range(0,256):
    b=bin(N)[2:]
    if len(b) < 8:
        b = (8 - len(b))*'0' + b
    b = b.replace('0', '-')
    b = b.replace('1', '0')
    b = b.replace('-', '1')
    R=int(b,2)
    
    if R-N == 133:
        print(N)
        break
